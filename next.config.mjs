/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/web', // Reemplaza 'nombre-del-repositorio' con el nombre de tu repositorio
  };
  
  export default nextConfig;